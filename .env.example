# ============================================================
# TACAI-Project — 环境配置模板
# 复制此文件为 .env.dev / .env.stg / .env.prd 并填入真实值
# ============================================================
NODE_ENV=development
PORT=3000
AUTH_PORT=3001

# PostgreSQL 数据库连接
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASS=your_database_password
